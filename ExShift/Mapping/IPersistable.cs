namespace ExShift.Mapping
{
    /// <summary>
    /// This interface requires the functionality for entities to be persistable (marker interface).
    /// </summary>
    public interface IPersistable
    {
    }
}
